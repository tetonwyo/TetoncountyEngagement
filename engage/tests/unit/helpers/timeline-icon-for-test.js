import {
  timelineIconFor
} from '../../../helpers/timeline-icon-for';
import { module, test } from 'qunit';

module('TimelineIconForHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timelineIconFor(42);
  assert.ok(result);
});
